namespace Pulsar

module Constants =
    open Types

    let MIN_ROOM_SIZE = 5
    let MAX_ROOM_SIZE = 15
    let MAX_NUM_ROOMS = 100
    let MAX_ROOM_AREA = 150

    let WORLD_SIZE = { Width = 200 ; Height = 200 }
